pages:
  stage: deploy
  script:
    - mkdir .public
    - cp -r images .public
    - cp -r css .public
    - cp -r SVG .public
    - cp index.html .public
    - rm -rf public
    - mv .public public
  artifacts:
    paths:
      - public
  only:
      - master